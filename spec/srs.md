# Software Requirements Specification
додана інфа від містер бренчі

## Інформаційна система контролю спортивної активності людини
додана інфа від тестової бренчі для мерж конфлікту)


| Поле | Значення |
|---|---|
| Автор | user12345 |
| Дата | 2026-09-18 |
| Версія документа | 1.0 |
| Статус | Draft — затверджено в ітеративному SKED-діалозі |


---

## 1. Призначення системи

Цей документ є формальною специфікацією вимог (Software Requirements Specification) для інформаційної системи контролю спортивної активності людини. Система забезпечує централізований облік виконаних вправ, витрачених хвилин і дат тренувань, а також вивід збереженої інформації на запит користувача.

### Проблеми, які розв'язує система

- Втрата записів про виконані тренування.
- Повільний пошук інформації про тренування.
- Відсутність контролю за кількістю та тривалістю спортивних справ.
- Незручний вивід збереженої інформації.

### Основні користувачі

Зареєстровані користувачі системи.

---

## 2. Межі системи

### In Scope

- **Управління користувачами**: реєстрація, автентифікація, ізоляція даних між користувачами.
- **Облік спортивних справ**: додавання запису з назвою вправи, кількістю хвилин, датою та нотатками.
- **Перегляд та вивід**: усі записи, записи за період, записи за назвою вправи, сумарна кількість хвилин.
- **Редагування та видалення** власних записів.

### Out of Scope

- Соціальні функції (обмін, рейтинги, публічні профілі).
- Розширена аналітика (графіки, рекомендації, інтеграція з фітнес-трекерами).
- Адміністрування (ієрархія ролей, панель адміністратора).
- Зовнішній обмін даними (експорт, інтеграції).

---

## 3. Терміни та визначення

| Термін | Визначення |
|---|---|
| Користувач | Зареєстрована фізична особа, яка має обліковий запис у системі та вносить дані про власну спортивну активність. |
| Спортивна справа (Workout Entry) | Запис у системі, що фіксує факт виконання певної вправи користувачем. |
| Вправа (Exercise) | Назва конкретного виду фізичної активності, зазначена користувачем у записі. |
| Тривалість (Duration) | Кількість хвилин, витрачених на виконання вправи. |
| Дата виконання (Performed Date) | Календарна дата, коли була виконана спортивна справа. |
| Запис (Record) | Одиниця збереженої інформації в базі даних, що відповідає одній спортивній справі. |
| База даних (Database) | Сховище, у якому централізовано зберігаються всі записи користувачів системи. |
| Вивід (Output) | Представлення збережених записів користувачеві у відповідь на його запит. |

---

## 4. Загальний опис системи

### Архітектура ролей

Система підтримує одну роль — **Користувач**, який має повний доступ лише до власних записів.

### Основні процеси

**Запис спортивної справи**
1. Авторизація користувача.
2. Відкриття форми додавання запису.
3. Введення назви вправи, кількості хвилин, дати.
4. Валідація введених даних.
5. Збереження запису з прив'язкою до користувача.

**Вивід записів**
1. Вибір параметрів виводу (усі записи, за період, за назвою вправи).
2. Вибірка лише записів, що належать поточному користувачеві.
3. Відображення списку записів.

**Редагування та видалення**
1. Вибір власного запису користувачем.
2. Перевірка належності запису користувачеві.
3. Виконання операції редагування або видалення.

---

## 5. Вимоги до інтерфейсів

### Інтерфейс користувача

- Форма реєстрації та входу.
- Форма додавання спортивної справи.
- Список збережених записів із фільтрацією.
- Форма редагування запису.
- Кнопка видалення запису.

### Загальні вимоги

- Веб-інтерфейс.
- Після авторизації користувачеві доступні лише власні записи.
- Inline-повідомлення про помилки (некоректна дата, від'ємна тривалість, порожня назва).
- Повідомлення про порожній список записів.

---

## 6. Обмеження

### Бізнес-обмеження

- Кожен запис належить рівно одному користувачеві.
- Ізоляція даних між користувачами обов'язкова.
- Дата виконання не може бути в майбутньому.
- Тривалість — додатне ціле число хвилин.
- Назва вправи не може бути порожньою.

### Технічні обмеження

- Кожен запис прив'язаний до ідентифікатора користувача.
- Паролі зберігаються у хешованому вигляді.
- Усі операції з записами виконуються лише після автентифікації.

### Обмеження процесу розробки (TDD)

- Тести пишуться до імплементації функціоналу.
- Кожна вимога з розділу 7, кожен критерій прийняття та BDD-сценарій з розділу 9 мають тестове покриття.
- Код без попереднього тесту не приймається.

### Обмеження простежуваності (SDD Logging)

- Усі зміни специфікації фіксуються в `/logs`.
- Підсумки SKED-діалогів фіксуються в `/logs`.
- Відхилені припущення моделей фіксуються в `/logs`.
- Результати контрольних перевірок фіксуються в `/logs`.
- Жодна суттєва зміна вимог не вноситься без відповідного запису в журналі.
- Записи в `/logs` є невід'ємною частиною артефактів проєкту.

---

## 7. Функціональні вимоги

### 7.1 Управління користувачами

| ID | Вимога |
|---|---|
| REQ-F-001 | Система повинна дозволяти реєстрацію нового користувача. |
| REQ-F-002 | Система повинна дозволяти вхід зареєстрованого користувача. |
| REQ-F-003 | Неавторизований користувач не повинен мати доступу до функцій системи. |
| REQ-F-004 | Користувач повинен мати доступ лише до власних записів. |

### 7.2 Облік спортивних справ

| ID | Вимога |
|---|---|
| REQ-F-010 | Система повинна дозволяти додавання нового запису про спортивну справу. |
| REQ-F-011 | Назва вправи є обов'язковим полем запису. |
| REQ-F-012 | Кількість хвилин є обов'язковим полем запису. |
| REQ-F-013 | Дата виконання є обов'язковим полем запису. |
| REQ-F-014 | Система повинна блокувати збереження запису з порожньою назвою вправи. |
| REQ-F-015 | Система повинна блокувати збереження запису з нульовою або від'ємною тривалістю. |
| REQ-F-016 | Система повинна блокувати збереження запису з датою виконання в майбутньому. |

### 7.3 Перегляд та вивід

| ID | Вимога |
|---|---|
| REQ-F-020 | Система повинна виводити список усіх власних записів користувача. |
| REQ-F-021 | Система повинна дозволяти вивід записів за вказаний період. |
| REQ-F-022 | Система повинна дозволяти вивід записів за назвою вправи. |
| REQ-F-023 | Система повинна виводити сумарну кількість хвилин за вказаний період. |
| REQ-F-024 | Вивід запису повинен містити назву вправи, тривалість та дату. |

### 7.4 Редагування та видалення

| ID | Вимога |
|---|---|
| REQ-F-030 | Система повинна дозволяти редагування власного запису користувача. |
| REQ-F-031 | Система повинна дозволяти видалення власного запису користувача. |
| REQ-F-032 | Система повинна блокувати доступ до редагування або видалення чужого запису. |

---

## 8. Нефункціональні вимоги

| ID | Категорія | Вимога |
|---|---|---|
| REQ-NF-001 | Безпека | Паролі користувачів повинні зберігатися з використанням bcrypt із cost-фактором не менше 10. |
| REQ-NF-002 | Безпека | Система повинна перевіряти належність запису користувачеві та повертати HTTP 403 при спробі доступу до чужого запису. |
| REQ-NF-003 | Продуктивність | Вивід списку записів повинен виконуватися не більше ніж за 2 секунди при обсязі бази у 10 000 записів. |
| REQ-NF-004 | Продуктивність | Збереження нового запису повинно виконуватися не більше ніж за 1 секунду. |
| REQ-NF-005 | Цілісність даних | Кожен запис обов'язково повинен бути прив'язаний до конкретного користувача. |

---

## 9. Критерії прийняття та BDD-сценарії

### 9.1 Критерії прийняття

**REQ-F-004 — Ізоляція даних**
- Користувач бачить у списку лише ті записи, які створив він сам.
- Записи інших користувачів не відображаються та не доступні за прямим запитом.

**REQ-F-014 — Порожня назва вправи**
- Спроба зберегти запис із порожнім полем назви завершується помилкою.
- Запис із порожньою назвою не потрапляє до бази даних.

**REQ-F-015 — Некоректна тривалість**
- Спроба зберегти запис із нульовою або від'ємною тривалістю завершується помилкою.
- Користувачеві відображається повідомлення про некоректне значення тривалості.

**REQ-F-016 — Дата в майбутньому**
- Спроба зберегти запис із датою виконання, пізнішою за поточну дату, завершується помилкою.
- Користувачеві відображається повідомлення про недопустиму дату.

**REQ-F-032 — Доступ до чужого запису**
- Спроба редагування запису, що належить іншому користувачеві, відхиляється системою.
- Спроба видалення запису, що належить іншому користувачеві, відхиляється системою.
- У відповідь повертається код HTTP 403.

### 9.2 BDD-сценарії (Gherkin)

**REQ-F-010 — Додавання спортивної справи**

```gherkin
Функціонал: Додавання запису про спортивну справу

  Сценарій: Успішне додавання запису
    Дано користувач авторизований у системі
    Коли користувач додає запис із назвою "Біг", тривалістю 30 хвилин та коректною датою
    Тоді запис зберігається в базі даних
    І запис прив'язується до поточного користувача

  Сценарій: Блокування запису з порожньою назвою
    Дано користувач авторизований у системі
    Коли користувач намагається додати запис із порожньою назвою вправи
    Тоді система відхиляє збереження запису
    І користувачеві відображається повідомлення про помилку
```

**REQ-F-020 — Вивід записів**

```gherkin
Функціонал: Вивід списку спортивних справ

  Сценарій: Успішний вивід записів
    Дано користувач авторизований у системі
    І у користувача є збережені записи
    Коли користувач запитує список своїх записів
    Тоді система відображає всі записи цього користувача
    І кожен запис містить назву вправи, тривалість та дату

  Сценарій: Вивід порожнього списку
    Дано користувач авторизований у системі
    І у користувача немає збережених записів
    Коли користувач запитує список своїх записів
    Тоді система відображає повідомлення про відсутність записів
```

**REQ-F-032 — Доступ до чужого запису**

```gherkin
Функціонал: Захист чужих записів від редагування та видалення

  Сценарій: Спроба редагування чужого запису
    Дано користувач авторизований у системі
    І існує запис, що належить іншому користувачеві
    Коли користувач намагається відредагувати цей запис
    Тоді система відхиляє операцію
    І повертає код HTTP 403

  Сценарій: Спроба видалення чужого запису
    Дано користувач авторизований у системі
    І існує запис, що належить іншому користувачеві
    Коли користувач намагається видалити цей запис
    Тоді система відхиляє операцію
    І повертає код HTTP 403
```
